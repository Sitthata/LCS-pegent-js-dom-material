{
  "name": "js-dom-material",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "bunx --watch json-server db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
